# Backend dev tooling: lint, format, test.
# Install with: pip install -r backend/requirements.txt -r backend/dev-requirements.txt
# Or via pyproject extras: pip install -e ".[dev]"
ruff==0.7.4
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
respx==0.21.1
